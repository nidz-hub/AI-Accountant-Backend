const USER_ID = "demo-user";

const TRANSACTION_TYPES = {
  SALE: "sale",
  EXPENSE: "expense",
  PURCHASE: "purchase"
};

const TRANSACTION_SOURCES = {
  PHOTO: "photo",
  VOICE: "voice",
  MANUAL: "manual"
};

const CURRENCY = "INR";

module.exports = {
  USER_ID,
  TRANSACTION_TYPES,
  TRANSACTION_SOURCES,
  CURRENCY
};